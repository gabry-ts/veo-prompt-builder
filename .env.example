# Default User Configuration
# These credentials will be used to create the first user when the application starts
# IMPORTANT: Change these values before deploying to production!
DEFAULT_USER_EMAIL=admin@example.com
DEFAULT_USER_PASSWORD=changeme123
DEFAULT_USER_NAME=Admin User

# Database Configuration (if running outside Docker)
DATABASE_URL=postgresql://meoveo:meoveo@localhost:5432/meoveo?schema=public

# JWT Configuration
# IMPORTANT: Generate a strong secret key for production!
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# Application Configuration
PORT=3000
NODE_ENV=development
